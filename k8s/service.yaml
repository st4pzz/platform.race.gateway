apiVersion: v1
kind: Service
metadata:
  name: store-gateway
  labels:
    run: store-gateway
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: store-gateway